<% if errors.any? %>
  <div class="alert alert-danger">
    <div class="lead buffer-bottom">
      В вашей форме <%= errors.count %> ошибок
    </div>

    <ul>
      <% errors.full_messages.each do |message| %>
        <li><%= t(message) %></li>
      <% end %>
    </ul>
  </div>
<% end %>